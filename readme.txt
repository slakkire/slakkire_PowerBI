Company Needs: 

1. Information Collecting or Requirement: Created a one-page dashboard with multiple representations to successfully display sales information. Go ahead and use your creative abilities to best convey the knowledge you have.

a.Sales:yearly
b.Categories,Geography,SalesRep,SubCategories:Excel
c.Product:CSV
1a:

Build a method that loads all of the documents from the marketing folder into one Sales database.
The setup should be robust, as deleting a document from the marketing folder fails to result in a warning for absent files. The addition of a new annual revenue record should immediately load it into the actual inquiry when it is refreshed.

2. Information-based analysis.

2a: Change its Sales database to separate the "Location" column by Country and City. Ensure certain that you've configured the right Data Category to support Geo maps.
    Apply the required changes in the Date column to ensure you can configure the Date style.
    
2b: Establish an individual GeoKey in the Sales and Geography tables.

Build a method that loads all of the documents from the marketing folder into one Sales database.
The system should be robust, as deleting an item from the marketing directory fails to result in a warning for absent files. The addition of a fresh annual revenue record should immediately load it into the actual query when it is refreshed.



2c: Develop an easy function that eliminates the "ID - " portion of both columns, which you may utilize and repeat for both of these searches to remove the IDs.Dimensional Searches. Sales Representative & Sub Division require extra care. Several ID columns use a particular structure:
                      ID-4,ID-5,ID-6

Develop an easy function that eliminates the "ID - " portion of both columns, which you may utilize and utilize for both of these searches for removing the IDs.

2d: Establish a Data Architecture that connects all fields and uses the Calendar column previously built up in my pbix.

3. DAX 

3a: For calculating the overall profit shown in the Sales table, multiply the price at which the item is sold by the number of sales.
3b: Estimate the overall price shown in the Sales column by multiplying the item's usual price by the number of units.
3c: Compute the Gross Profit in Purchases by subtracting the entire revenue from the Overall Price.
3d: Compute a gross revenue MoM Increase Change% statistic to aid decision-making.
3e: Compute AVG revenues per day, which is the mean total earnings per day determined by real sale days.
3f: Break the Study by Product (decrease or growth)Compute the subsequent time measurements: This document is a QBR report. So, QoQ increase is essential.

4. Build sales data with different pictures to effectively display Sales Data on a single-screen interface. Go ahead to utilize your creative abilities to best reflect the information you have accessible.
If you graph months along the x-axis of the chart, ensure they are ordered from Jan to Dec.

